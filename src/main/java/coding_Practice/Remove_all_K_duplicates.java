package coding_Practice;

import java.util.ArrayList;
import java.util.Stack;

public class Remove_all_K_duplicates {
	/*public String removeDuplicates(String s, int k) {
		Stack<Character> sChar=new Stack<>();
		Stack<Integer> sCount=new Stack<>();
		ArrayList<Object> Arrobj = new ArrayList<>();
		//Arrobj.
		//int count;
		for(int i=0;i<s.length();i++) {
			if(!(sChar.isEmpty())&&sChar.peek()==s.charAt(i)) {
				System.out.println(sCount.peek());
				count=sCount.peek();
				sCount.push(count+1);
				}
			else {
			  sCount.push(1234);}
			  sChar.push(s.charAt(i));
		     if(!sCount.isEmpty()&&sCount.pop()==3){
				for(int j=0;j<k;j++) {
					sChar.pop();
					sCount.pop();
				}
			   }
		      }
		StringBuilder st= new StringBuilder();
		while(!sChar.empty()) {
		  st.append(sChar.pop());
		}return st.reverse().toString();
	 }
	public static void main(String[] args) {
		String s="eeecf";
		Remove_all_K_duplicates obj=new Remove_all_K_duplicates();
		String rslt=obj.removeDuplicates(s,3);
		System.out.println(rslt);
	}*/
	
	
	
  }
